---
phase: 48
plan: 2
wave: 2
---

# Plan 48.2: Geolocalización y Mapas

## Objective
Visualizar los terrenos en un mapa interactivo (vista global) y permitir seleccionar coordenadas al crear o editar un terreno.

## Context
- src/modules/client/realestate/RealEstateModule.jsx
- src/modules/client/realestate/components/TerrainFormModal.jsx
- src/modules/client/realestate/components/MapViewer.jsx (nuevo)

## Tasks

<task type="auto">
  <name>Create Map Component</name>
  <files>src/modules/client/realestate/components/MapViewer.jsx</files>
  <action>
    Crear un componente `MapViewer` utilizando `react-leaflet`.
    - Debe recibir un arreglo de `terrains` (con coords) o un solo `terrain`.
    - Mostrar un pin (Marker) por terreno.
    - Si se le pasa `onLocationSelect`, debe permitir hacer clic en el mapa para capturar `lat` y `lng` (útil para el formulario).
  </action>
  <verify>Test-Path "src/modules/client/realestate/components/MapViewer.jsx"</verify>
  <done>Componente MapViewer creado exitosamente.</done>
</task>

<task type="auto">
  <name>Integrate Map in UI</name>
  <files>src/modules/client/realestate/RealEstateModule.jsx, src/modules/client/realestate/components/TerrainFormModal.jsx</files>
  <action>
    1. En `RealEstateModule.jsx`, añadir una vista "Mapa" junto a las vistas actuales de listado/kanban, o incrustar un botón para ver el mapa general.
    2. En `TerrainFormModal.jsx`, añadir campos (inputs read-only) para Latitud/Longitud y un botón "Seleccionar en Mapa" que despliegue el `MapViewer` para hacer el picking de la ubicación.
  </action>
  <verify>Select-String -Path "src/modules/client/realestate/RealEstateModule.jsx" -Pattern "MapViewer" -Quiet</verify>
  <done>Los mapas están incrustados en la vista principal y en el modal de edición.</done>
</task>

## Success Criteria
- [ ] Es posible elegir la ubicación en un mapa al crear un terreno.
- [ ] El dashboard de Terrenos puede mostrar todos los terrenos geolocalizados.
