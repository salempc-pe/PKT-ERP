---
phase: 55
plan: 1
wave: 1
---

# Plan 55.1: Homologación Rígida de Orden de Módulos

## Objective
Forzar que el orden de las tarjetas en el Dashboard y los ítems del Sidebar sigan estrictamente el orden definido en `MODULES_CATALOG`, ignorando configuraciones personalizadas previas que causan desincronización, y depurar claves duplicadas (como la doble tarjeta de Bodega).

## Context
- d:\Users\Usuario\Documents\TRABAJO\PROYECTOS\PKT ERP\src\modules\modulesConfig.js
- d:\Users\Usuario\Documents\TRABAJO\PROYECTOS\PKT ERP\src\modules\client\dashboard\DashboardSettingsModal.jsx

## Tasks

<task type="auto">
  <name>Refactorizar lógica de ordenamiento y deduplicación</name>
  <files>d:\Users\Usuario\Documents\TRABAJO\PROYECTOS\PKT ERP\src\modules\modulesConfig.js</files>
  <action>
    - Modifica `getAccessibleModules` para retornar `[...new Set(accessible)]` asegurando que no existan duplicados (previene doble tarjeta de Bodega).
    - Sobrescribe `getOrderedModules(accessibleKeys, savedOrder)` para que devuelva directamente el mapeo de `MODULES_CATALOG.map(m => m.id).filter(id => accessibleKeys.includes(id))`, eliminando por completo cualquier lógica que respete `savedOrder`.
  </action>
  <verify>Observar código final y verificar validación de sintaxis en el compilador Vite.</verify>
  <done>getOrderedModules aplica rígidamente el orden de MODULES_CATALOG a las accessibleKeys deduplicadas.</done>
</task>

<task type="auto">
  <name>Eliminar controles manuales de reordenamiento del usuario</name>
  <files>d:\Users\Usuario\Documents\TRABAJO\PROYECTOS\PKT ERP\src\modules\client\dashboard\DashboardSettingsModal.jsx</files>
  <action>
    - Eliminar todo el div marcado como `{/* Reorder Controls */}` (botones con ChevronUp y ChevronDown) y remover la función `moveModule` que manipulaba esto.
    - Como el orden ya no es reordenable interactivamente, inicializar el estado `orderedModules` derivando directamente del orden centralizado `getOrderedModules(accessibleKeys)` (al cual ya no se le debe pasar la preferencia guardada, simplemente pasar accessibleKeys) o `const orderedModules = getOrderedModules(accessibleKeys)` calculada en render, y eliminar llamadas a `setOrderedModules` de este componente.
    - En el método `handleSave`, eliminar el envío de `modulesOrder` dentro del payload `finalData` a Firestore, ya que el orden ahora es fijo.
  </action>
  <verify>Validar que la pestaña de opciones guarde exitosamente y renderice sin errores.</verify>
  <done>Los controles de reordenamiento desaparecieron de la UI del Modal y no se guarda modulesOrder en Firestore.</done>
</task>

## Success Criteria
- [ ] No existen tarjetas duplicadas en el Dashboard.
- [ ] Sidebar y Dashboard se adhieren al orden estricto de la lista de configuración (catálogo).
- [ ] Modal de Dashboard ya no muestra los iconos de arriba y abajo para organizar módulos.
