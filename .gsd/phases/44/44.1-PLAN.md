---
phase: 44
plan: 1
wave: 1
---

# Plan 44.1: Refactorización de Datos (Bodega)

## Objective
Adaptar el hook actual de Bodega para soportar operaciones multi-almacén (vinculando con la colección global `warehouses`) e implementar un nuevo hook para gestionar los umbrales de stock bajo por ubicación.

## Context
- .gsd/SPEC.md
- .gsd/phases/44/DESIGN.md
- src/modules/client/warehouse/useWarehouse.js
- src/modules/client/inventory/useWarehouses.js

## Tasks

<task type="auto">
  <name>Refactorizar useWarehouse.js</name>
  <files>
    - src/modules/client/warehouse/useWarehouse.js
  </files>
  <action>
    - Modificar `useWarehouse` para aceptar opcionalmente un `selectedWarehouseId` y filtrar las consultas de `stock` e `history` por este ID si se provee.
    - Modificar la función `addMovement` para recibir `warehouseId` dentro de `movementData` y guardarlo tanto en la colección `warehouse_history` como en `warehouse_stock` (en ingresos).
    - En `addMovement`, cuando es salida ('OUT'), verificar y descontar la cantidad, pero en `warehouse_history` guardar además el `movementValue` (quantity * price, buscando el precio de compra original del lote).
  </action>
  <verify>grep -n "warehouseId" src/modules/client/warehouse/useWarehouse.js</verify>
  <done>El hook useWarehouse permite guardar y filtrar movimientos por warehouseId, y calcula el movementValue en las salidas.</done>
</task>

<task type="auto">
  <name>Implementar useMaterialSettings</name>
  <files>
    - src/modules/client/warehouse/useMaterialSettings.js
  </files>
  <action>
    - Crear un nuevo hook `useMaterialSettings(orgId)` para gestionar la colección `organizations/{orgId}/material_settings`.
    - Debe retornar la lista de configuraciones y funciones para agregar/actualizar: `updateThreshold(materialName, warehouseId, threshold)`.
    - La estructura del documento debe seguir el diseño: `{ materialName, thresholds: { [warehouseId]: number } }`.
  </action>
  <verify>cat src/modules/client/warehouse/useMaterialSettings.js | grep -n "updateThreshold"</verify>
  <done>Existe un hook useMaterialSettings capaz de leer y guardar umbrales por material y almacén.</done>
</task>

## Success Criteria
- [ ] Los movimientos de bodega se registran con su respectivo `warehouseId`.
- [ ] Se puede configurar y guardar un umbral de stock mínimo por material y almacén.
