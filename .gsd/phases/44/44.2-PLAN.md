---
phase: 44
plan: 2
wave: 2
---

# Plan 44.2: UI Multi-Almacén y Valorización (Bodega)

## Objective
Actualizar la interfaz del módulo de Bodega para integrar el selector de almacenes, calcular el valor del inventario en tiempo real y mostrar alertas visuales basadas en los umbrales configurados.

## Context
- .gsd/phases/44/DESIGN.md
- src/modules/client/warehouse/WarehouseModule.jsx
- src/modules/client/warehouse/WarehouseDashboardCard.jsx

## Tasks

<task type="auto">
  <name>Actualizar UI de WarehouseModule</name>
  <files>
    - src/modules/client/warehouse/WarehouseModule.jsx
  </files>
  <action>
    - Importar y usar `useWarehouses(orgId)` para obtener la lista de almacenes globales.
    - Añadir un `<select>` o un conjunto de tabs/botones rápidos para filtrar la vista actual por un Almacén específico.
    - Modificar el formulario del Modal de Movimiento para exigir la selección de un `warehouseId` de destino/origen.
    - Importar y usar `useMaterialSettings` para cruzar la data de stock con los umbrales.
  </action>
  <verify>grep -n "useWarehouses" src/modules/client/warehouse/WarehouseModule.jsx</verify>
  <done>El módulo de bodega permite filtrar visualmente por almacén y seleccionar almacén en los ingresos/salidas.</done>
</task>

<task type="auto">
  <name>Desarrollar Lógica de Valorización y Alertas</name>
  <files>
    - src/modules/client/warehouse/WarehouseModule.jsx
  </files>
  <action>
    - Añadir una sección de "Indicadores" en la cabecera (similar a Inventario) que muestre: "Inversión Total" (suma de quantity * purchasePrice de todos los lotes del almacén seleccionado).
    - En la tabla de Stock Actual, añadir un indicador visual (ej. icono rojo) si el stock total de un material en el almacén seleccionado es menor al `threshold` definido en `useMaterialSettings`.
    - Proporcionar un pequeño botón al lado del material para "Configurar mínimo" que abra un modal rápido para establecer el umbral en ese almacén.
  </action>
  <verify>grep -n "Inversión" src/modules/client/warehouse/WarehouseModule.jsx</verify>
  <done>La interfaz muestra la inversión valorizada y alerta visualmente sobre materiales con stock bajo.</done>
</task>

## Success Criteria
- [ ] El usuario puede filtrar todo el stock de bodega seleccionando un Almacén.
- [ ] La cabecera muestra la suma de valor invertido en los insumos mostrados.
- [ ] Los materiales por debajo de su umbral en un almacén muestran una alerta visual clara.
