---
phase: 43
plan: 3
wave: 2
---

# Plan 43.3: Interfaz de Almacenes, Escaneo QR/Barras y Transferencias

## Objective
Adaptar la UI del módulo de inventario para gestionar almacenes, realizar transferencias con interfaz visual, e incorporar soporte de lectura de códigos de barras (físico o cámara).

## Context
- .gsd/SPEC.md
- src/modules/client/inventory/InventoryModule.jsx

## Tasks

<task type="auto">
  <name>Soporte de Escaneo de Códigos de Barras</name>
  <files>
    - package.json
    - src/modules/client/inventory/BarcodeScanner.jsx
  </files>
  <action>
    - Instalar el paquete `html5-qrcode` para lectura mediante cámara web/móvil.
    - Crear componente `BarcodeScanner.jsx` que exponga 1) un Input text invisible o auto-focado para lectores USB (teclado). 2) Un botón que abre un modal con la cámara para escaneo QR/Barra visual.
    - El componente debe emitir la función `onScan(codigo)` cuando un código es procesado.
  </action>
  <verify>grep_search() en el componente BarcodeScanner.jsx para verificar imports y uso de html5-qrcode. npm list html5-qrcode para verificar dependencias.</verify>
  <done>El usuario puede escanear un código de barras usando un lector físico (USB) o la cámara de su dispositivo.</done>
</task>

<task type="auto">
  <name>Actualizar InventoryModule UI (Tabs y Modales)</name>
  <files>
    - src/modules/client/inventory/InventoryModule.jsx
  </files>
  <action>
    - Añadir soporte de "Pestañas" al nivel superior del Módulo de Inventario: "Productos", "Almacenes", "Transferencias".
    - En la pestaña de Productos: Modificar la creación/edición de producto para soportar ingreso por almacén (usando el array de almacenes cargados de `useWarehouses`).
    - En la vista de tabla, mostrar el Costo Promedio (averageCost) y el stock total agrupado.
    - Integrar `BarcodeScanner` en la barra de búsqueda para que, al escanear, filtre inmediatamente o abra el modal de transferencia/salida del producto escaneado.
    - Implementar UI básica en pestaña "Transferencias" para iniciar (con un producto y dos almacenes) y confirmar recepciones.
  </action>
  <verify>Verificación manual de la UI mostrando las 3 pestañas y renderizando el botón de "Escanear".</verify>
  <done>La UI es funcional, los almacenes se listan correctamente, y el escáner reacciona filtrando el inventario.</done>
</task>

## Success Criteria
- [ ] Los usuarios pueden crear un segundo almacén y realizar transferencias desde el primero.
- [ ] Un escáner USB emitiendo códigos de barra busca/selecciona productos al instante en la lista.
