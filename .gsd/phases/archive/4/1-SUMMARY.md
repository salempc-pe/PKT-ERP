# Plan 4.1 Summary

**Status**: ✅ Complete

**Tasks Completed**:
1. Creada la capa de datos de ventas mediante `useSales.js`.
2. Se habilitó el soporte para simular datos en caso no se tenga la clave de Firebase usando la misma variable `isFirebaseConfigured`.
3. Retorna la data en objeto reactivo incluyendo las operaciones `addSale` y `updateSaleStatus` validando la colección de Firestore en base al `orgId`.

**Verification**:
El archivo ha sido guardado exitosamente en `src/hooks/useSales.js` con las exportaciones correctas.
