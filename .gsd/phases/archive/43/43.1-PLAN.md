---
phase: 43
plan: 1
wave: 1
---

# Plan 43.1: Modelos de Datos y Gestión de Almacenes

## Objective
Actualizar el esquema de productos para soportar múltiples almacenes y costos promedio, y crear el sistema base para gestionar los almacenes físicos o lógicos de la organización.

## Context
- .gsd/SPEC.md
- .gsd/phases/43/RESEARCH.md
- src/modules/client/inventory/useInventory.js

## Tasks

<task type="auto">
  <name>Actualizar Esquema de Producto en useInventory.js</name>
  <files>
    - src/modules/client/inventory/useInventory.js
  </files>
  <action>
    - Añadir al `ProductSchema` los campos `averageCost` (number, default 0) y `stockByWarehouse` (record o map de strings a numbers, opcional).
    - Asegurar que al crear un producto (addProduct), si no se provee `averageCost`, inicie en 0.
    - Asegurar compatibilidad hacia atrás: si `stockByWarehouse` no existe, que falle de forma grácil o se inicialice vacío.
  </action>
  <verify>grep_search("averageCost" in useInventory.js) && grep_search("stockByWarehouse" in useInventory.js)</verify>
  <done>El esquema de zod y la lógica de creación/actualización admiten averageCost y stockByWarehouse de forma segura.</done>
</task>

<task type="auto">
  <name>Crear Hook useWarehouses y Lógica Base</name>
  <files>
    - src/modules/client/inventory/useWarehouses.js
  </files>
  <action>
    - Crear el hook `useWarehouses(orgId)` para leer/escribir en la colección `organizations/{orgId}/warehouses`.
    - Estructura del almacén: `id`, `name` (string), `location` (string), `isDefault` (boolean), `status` (active/inactive).
    - Implementar onSnapshot, addWarehouse, updateWarehouse, deleteWarehouse.
    - Asegurar que al crear el primer almacén, automáticamente sea `isDefault: true`.
  </action>
  <verify>Test de que `useWarehouses.js` se ha creado y exporta las funciones correctas mediante cat/grep.</verify>
  <done>Hook useWarehouses creado con soporte en tiempo real y CRUD para almacenes.</done>
</task>

## Success Criteria
- [ ] El módulo de inventario actual no se rompe y soporta los nuevos campos.
- [ ] Existe una base de datos de almacenes accesible mediante el hook `useWarehouses`.
