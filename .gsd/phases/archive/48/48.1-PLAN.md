---
phase: 48
plan: 1
wave: 1
---

# Plan 48.1: Schema Updates & Dependencies

## Objective
Actualizar el esquema de datos de Terrenos para soportar coordenadas de geolocalización, presentaciones (compradores potenciales) y documentos legales. Además, instalar las librerías necesarias para el mapa.

## Context
- .gsd/SPEC.md
- src/modules/client/realestate/useRealEstate.js

## Tasks

<task type="auto">
  <name>Install Leaflet Dependencies</name>
  <files>package.json</files>
  <action>
    Instalar las librerías `leaflet` y `react-leaflet` vía npm/pnpm.
    Asegúrate de agregar también la configuración de CSS de leaflet en el `index.html` o `index.css` si es necesario, o importarlo en el entrypoint.
  </action>
  <verify>Get-Content package.json | Select-String "react-leaflet"</verify>
  <done>Dependencias agregadas a package.json</done>
</task>

<task type="auto">
  <name>Update TerrainSchema</name>
  <files>src/modules/client/realestate/useRealEstate.js</files>
  <action>
    Actualizar `TerrainSchema` (usando Zod):
    1. Añadir `coordinates: z.object({ lat: z.number(), lng: z.number() }).optional()`.
    2. Añadir `presentations: z.array(z.object({ id: z.string(), buyerId: z.string(), status: z.enum(['interes', 'visita', 'propuesta', 'rechazado']), notes: z.string().optional(), date: z.string() })).default([])`.
    3. Añadir `documents: z.array(z.object({ id: z.string(), name: z.string(), type: z.string(), url: z.string(), uploadedAt: z.string() })).default([])`.
  </action>
  <verify>Get-Content src/modules/client/realestate/useRealEstate.js | Select-String "coordinates"</verify>
  <done>El esquema de Zod permite los nuevos campos de forma opcional o con valores por defecto.</done>
</task>

## Success Criteria
- [ ] Zod schema soporta `coordinates`, `presentations` y `documents`.
- [ ] `react-leaflet` y `leaflet` están en `package.json`.
