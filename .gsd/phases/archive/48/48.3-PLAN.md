---
phase: 48
plan: 3
wave: 2
---

# Plan 48.3: Seguimiento de Presentaciones

## Objective
Implementar una vista detallada para cada terreno que permita llevar un historial/registro de a qué clientes (compradores potenciales) se les ha presentado el terreno y en qué estatus se encuentra esa negociación específica.

## Context
- src/modules/client/realestate/RealEstateModule.jsx
- src/modules/client/realestate/components/TerrainDetailsModal.jsx (nuevo)

## Tasks

<task type="auto">
  <name>Create Terrain Details Modal</name>
  <files>src/modules/client/realestate/components/TerrainDetailsModal.jsx, src/modules/client/realestate/RealEstateModule.jsx</files>
  <action>
    Crear un modal `TerrainDetailsModal` que se abra al hacer clic en un terreno (desde la lista o el kanban).
    Este modal debe mostrar la información básica del terreno y tener pestañas (Tabs) o secciones para "Presentaciones" y "Documentos".
    Integrar la apertura de este modal en `RealEstateModule.jsx`.
  </action>
  <verify>Test-Path "src/modules/client/realestate/components/TerrainDetailsModal.jsx"</verify>
  <done>El modal de detalles de terreno es invocable.</done>
</task>

<task type="auto">
  <name>Implement Presentations Tab</name>
  <files>src/modules/client/realestate/components/TerrainDetailsModal.jsx</files>
  <action>
    Dentro de `TerrainDetailsModal`, en la sección "Presentaciones", renderizar una lista con las `presentations` del terreno.
    Crear un mini-formulario allí mismo (o un botón "Nueva Presentación") que permita:
    - Seleccionar un comprador (Dropdown desde `useCrm` leads/clientes, o simplemente un campo de texto si no está hookeado). Se recomienda usar `useCrm` para obtener la lista de contactos.
    - Seleccionar un status (`interes`, `visita`, `propuesta`, `rechazado`).
    - Añadir una nota y fecha.
    Al guardar, actualizar el array `presentations` del terreno llamando a `updateTerrain(terrain.id, { presentations: [...newPresentations] })`.
  </action>
  <verify>Select-String -Path "src/modules/client/realestate/components/TerrainDetailsModal.jsx" -Pattern "presentations" -Quiet</verify>
  <done>Se pueden registrar presentaciones a diferentes contactos para un mismo terreno.</done>
</task>

## Success Criteria
- [ ] La UI permite ver el historial completo de a quién se ha ofrecido el terreno.
- [ ] Es posible añadir un nuevo registro de presentación a un comprador potencial.
