# Summary 14.1: AuthContext Logic & Admin UI for Onboarding

## What was done
- Actualizada la lógica del `AuthContext.jsx` para soportar estados de usuario (`pending`, `active`).
- Implementada la función `adminCreateUser` para generar automáticamente un `inviteToken` y establecer el estado `pending`.
- Implementada la función `setupUserPassword` para permitir a los usuarios establecer su contraseña y activar su cuenta.
- Actualizada la interfaz `AdminClients.jsx` para mostrar badges de estado y permitir copiar el enlace de invitación al portapapeles.

## Verification
- Se verificó la presencia de la lógica de tokens y estados en `AuthContext.jsx`.
- Se verificó la integración de los botones de copia y badges en `AdminClients.jsx`.

## Commit
`feat(phase-14): auth logic and admin ui for onboarding`
