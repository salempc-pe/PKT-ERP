---
phase: 53
plan: 1
wave: 1
---

# Plan 53.1: Salud - Capa de Persistencia y Registro

## Objective
Implementar el hook `useHealth` que maneje la persistencia de datos en Firestore para las colecciones del módulo Salud bajo un entorno multi-tenant, y registrar el módulo en el ruteo principal.

## Context
- .gsd/SPEC.md
- .agents/workflows/register-module.md
- src/modules/client/crm/useCrm.js (ejemplo de patrones de hook)
- src/App.jsx (ruteo)

## Tasks

<task type="auto">
  <name>Crear hook useHealth.js</name>
  <files>src/modules/client/health/useHealth.js</files>
  <action>
    Crear el hook de persistencia que implemente la suscripción y operaciones CRUD para:
    - `expedientes`: Con filtro por client_id y estado.
    - `citas`: Orden cronológico.
    - `notas_sesion`
    - `notas_generales`
    - `archivos_paciente`
    Todas deben estar anidadas bajo `organizations/${orgId}/[collection]`.
    Integrar validación con Zod para los esquemas definidos en el prompt.
  </action>
  <verify>El archivo existe y exporta las funciones necesarias para interactuar con Firestore.</verify>
  <done>El hook compila sin errores sintácticos y sigue los patrones de multi-tenancy del ERP.</done>
</task>

<task type="auto">
  <name>Registrar módulo en Layout y App</name>
  <files>
    src/App.jsx
    src/layouts/client/ClientLayout.jsx
  </files>
  <action>
    - Crear el directorio `src/modules/client/health/` y un placeholder `HealthDashboard.jsx`.
    - Registrar el path `/client/salud` en App.jsx y apuntar al Dashboard.
    - Registrar el link "Salud" en el menú lateral (Sidebar) del `ClientLayout.jsx` usando un icono de salud (ej: `Activity` o `HeartPulse`).
  </action>
  <verify>El sidebar muestra el nuevo acceso directo.</verify>
  <done>Navegar a /client/salud carga el placeholder del módulo.</done>
</task>

## Success Criteria
- [ ] Hook de datos listo para usar.
- [ ] Enrutamiento funcional.
