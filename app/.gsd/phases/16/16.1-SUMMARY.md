# Plan 16.1 Summary: Capa de datos para Facturación y Vencimientos
- Se actualizó el hook `useSales.js`.
- Los datos mock ahora incluyen `documentType`, `issueDate` y `dueDate`.
- La función `addSale` (tanto mock como real) ahora gestiona estos campos, asignando valores por defecto si no se proporcionan.
